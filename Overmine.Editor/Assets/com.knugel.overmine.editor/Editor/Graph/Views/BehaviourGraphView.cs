using GraphProcessor;
using UnityEditor;

namespace Editor.Graph.Views
{
    public class BehaviourGraphView : BaseGraphView
    {
        public BehaviourGraphView(EditorWindow window) : base(window) { }
    }
}